# 增强诊断功能使用指南

## 📋 概述

增强诊断功能是对 `unified_diagnostics.py` 的扩展，用于检测影响轨迹跟踪但未被标准诊断覆盖的关键参数。

### 新增诊断内容

1. **MPC 权重分析** ⭐⭐⭐
   - 分析 position, velocity, heading, control_accel, control_alpha 权重是否合理
   - 通过跟踪误差和控制平滑性的权衡来判断
   - 自动给出权重调整建议

2. **一致性检查性能分析** ⭐⭐⭐
   - 统计一致性检查的拒绝率
   - 分析 alpha 值分布
   - 判断阈值是否过严

3. **控制平滑性分析** ⭐⭐
   - 分析控制输出的变化率
   - 检测控制抖动问题
   - 建议平滑性参数调整

4. **状态机切换分析** ⭐⭐
   - 统计控制器状态切换频率
   - 分析 MPC ↔ Backup 切换原因
   - 建议状态机参数调整

---

## 🚀 安装步骤

### 步骤 1: 复制增强诊断文件

```bash
# 确保在 controller_ros/scripts 目录下
cd ~/AllControlBase/controller_ros/scripts

# 确认文件存在
ls -l enhanced_diagnostics.py
ls -l apply_enhanced_diagnostics.py
```

### 步骤 2: 应用增强诊断补丁

```bash
# 自动应用补丁（会自动备份原文件）
python3 apply_enhanced_diagnostics.py
```

输出示例：
```
======================================================================
  增强诊断补丁应用工具
======================================================================

目标文件: /home/user/AllControlBase/controller_ros/scripts/unified_diagnostics.py
✅ 已备份原文件到: unified_diagnostics.py.backup_20241229_150000

📝 应用的修改:
  ✅ 第 85 行: 添加增强诊断导入
  ✅ 第 905 行: 添加增强分析器初始化
  ✅ 第 1530 行: 添加增强分析器初始化代码
  ✅ 第 1545 行: 修改为循环收集并实时分析
  ✅ 第 1560 行: 添加增强分析执行代码
  ✅ 第 2080 行: 添加增强诊断显示代码

💾 保存修改到: unified_diagnostics.py

======================================================================
  ✅ 增强诊断补丁应用成功!
======================================================================
```

### 步骤 3: 验证安装

```bash
# 检查导入是否成功
python3 -c "from enhanced_diagnostics import EnhancedDiagnostics; print('✅ 增强诊断模块可用')"
```

---

## 📊 使用方法

### 方法 1: 集成在 unified_diagnostics.py 中使用（推荐）

```bash
# 运行增强诊断（需要控制器运行）
rosrun controller_ros unified_diagnostics.py \
  --mode tuning \
  --runtime-tuning \
  --duration 60 \
  --output ~/moshi/enhanced_config.yaml \
  --log-file ~/moshi/enhanced_diagnostics.log
```

### 方法 2: 独立运行增强诊断

```bash
# 启动控制器
roslaunch controller_ros turtlebot1.launch

# 在另一个终端运行增强诊断
rosrun controller_ros enhanced_diagnostics.py --duration 60
```

---

## 📈 诊断报告解读

### 示例报告

```
======================================================================
  增强诊断报告 (Enhanced Diagnostics Report)
======================================================================

【1. MPC 权重分析】
  横向误差: avg=0.099m, max=0.338m
  航向误差: avg=6.9°
  加速度变化: avg=1.23 m/s²/s, max=4.56 m/s²/s
  角加速度变化: avg=2.34 rad/s²/s, max=8.90 rad/s²/s

  建议:
    ✅ mpc.weights
       问题: 无
       建议: 权重设置良好，跟踪精度和平滑性平衡

【2. 一致性检查分析】
  拒绝率: 2.3% (23/1000)
  Alpha: avg=0.987, min=0.456

  建议:
    ℹ️ consistency check
       问题: 无
       建议: 一致性检查性能良好 (拒绝率 2.3%)

【3. 控制平滑性分析】
  线速度: std=0.045 m/s, avg_change=0.012 m/s
  角速度: std=0.123 rad/s, avg_change=0.034 rad/s
  最大变化: vx=0.089 m/s, omega=0.234 rad/s

  建议:
    ✅ control smoothness
       问题: 无
       建议: 控制输出非常平滑

【4. 状态机切换分析】
  切换次数: 0

  建议:
    ✅ state machine
       问题: 无
       建议: 无状态切换，控制器稳定运行

======================================================================
```

### 报告解读

#### 1. MPC 权重分析

**关键指标**：
- `横向误差`: 应该 < 0.15m (avg), < 0.40m (max)
- `航向误差`: 应该 < 10° (avg)
- `加速度变化`: 应该 < 5.0 m/s²/s (max)
- `角加速度变化`: 应该 < 10.0 rad/s²/s (max)

**常见问题**：

| 症状 | 原因 | 建议 |
|------|------|------|
| 误差大 + 控制平滑 | position/heading 权重过低 | 增加 position: 10→15, heading: 5→8 |
| 误差小 + 控制抖动 | control_accel/alpha 权重过低 | 增加 control_accel: 0.2→0.5 |
| 误差大 + 控制抖动 | 权重不平衡 | 同时调整跟踪和平滑权重 |

#### 2. 一致性检查分析

**关键指标**：
- `拒绝率`: 应该 < 5%
- `min_alpha`: 应该 > 0.3

**常见问题**：

| 拒绝率 | 评估 | 建议 |
|--------|------|------|
| < 5% | ✅ 良好 | 保持当前设置 |
| 5-10% | ⚠️ 偏高 | 考虑放宽阈值 |
| > 10% | 🔴 过高 | 放宽 kappa_thresh: 0.5→0.7, v_dir_thresh: 0.8→0.9 |

**如果 min_alpha < 0.2**：
- 🔴 严重问题：轨迹质量差
- 检查轨迹发布节点
- 检查网络延迟

#### 3. 控制平滑性分析

**关键指标**：
- `max_vx_change`: 应该 < 0.2 m/s
- `max_omega_change`: 应该 < 0.5 rad/s

**常见问题**：

| 症状 | 原因 | 建议 |
|------|------|------|
| 线速度跳变大 | control_accel 权重过低 | 增加 control_accel 权重 |
| 角速度跳变大 | control_alpha 权重过低 | 增加 control_alpha 权重 |
| 整体抖动 | 过渡参数不当 | 调整 transition.tau |

#### 4. 状态机切换分析

**关键指标**：
- `切换频率`: 应该 < 0.1 次/秒
- `MPC→Backup 切换`: 应该 = 0

**常见问题**：

| 切换频率 | 评估 | 建议 |
|----------|------|------|
| 0 次/秒 | ✅ 完美 | 控制器稳定 |
| < 0.1 次/秒 | ✅ 良好 | 偶尔切换可接受 |
| 0.1-0.5 次/秒 | ⚠️ 频繁 | 检查 MPC 参数 |
| > 0.5 次/秒 | 🔴 过于频繁 | 调整状态机参数 |

---

## 🔧 根据诊断结果调整配置

### 场景 1: 跟踪误差大但控制平滑

**诊断结果**：
```
横向误差: avg=0.180m, max=0.450m
加速度变化: avg=0.80 m/s²/s, max=2.10 m/s²/s
```

**问题**: position 权重过低

**解决方案**：
```yaml
mpc:
  weights:
    position: 15.0  # 从 10.0 增加
    heading: 8.0    # 从 5.0 增加
```

### 场景 2: 控制输出抖动

**诊断结果**：
```
最大变化: vx=0.250 m/s, omega=0.680 rad/s
```

**问题**: control_accel/alpha 权重过低

**解决方案**：
```yaml
mpc:
  weights:
    control_accel: 0.5  # 从 0.2 增加
    control_alpha: 0.5  # 从 0.2 增加
```

### 场景 3: 一致性检查拒绝率高

**诊断结果**：
```
拒绝率: 12.5% (125/1000)
```

**问题**: 一致性阈值过严

**解决方案**：
```yaml
consistency:
  kappa_thresh: 0.7           # 从 0.5 放宽
  v_dir_thresh: 0.9           # 从 0.8 放宽
  temporal_smooth_thresh: 0.5 # 从 0.4 放宽
```

### 场景 4: 频繁状态切换

**诊断结果**：
```
切换频率: 0.8 次/秒
MPC_CONTROL → BACKUP_CONTROL: 48 次
```

**问题**: MPC 不稳定或状态机参数不当

**解决方案**：
```yaml
safety:
  state_machine:
    mpc_fail_thresh: 5          # 从 3 增加（更宽容）
    mpc_recovery_thresh: 3      # 从 5 减少（更快恢复）
```

---

## 🎯 完整工作流程

### 1. 首次部署

```bash
# 步骤 1: 基础调优
rosrun controller_ros unified_diagnostics.py \
  --mode tuning \
  --duration 300 \
  --output ~/moshi/base_config.yaml

# 步骤 2: 底盘测试
rosrun controller_ros unified_diagnostics.py \
  --mode tuning \
  --test-chassis \
  --output ~/moshi/chassis_config.yaml

# 步骤 3: 运行时增强诊断
roslaunch controller_ros turtlebot1.launch config:=~/moshi/chassis_config.yaml

rosrun controller_ros unified_diagnostics.py \
  --mode tuning \
  --runtime-tuning \
  --duration 60 \
  --output ~/moshi/final_config.yaml \
  --log-file ~/moshi/enhanced_diag.log
```

### 2. 查看增强诊断报告

```bash
# 查看日志文件中的增强诊断部分
grep -A 50 "增强诊断报告" ~/moshi/enhanced_diag.log
```

### 3. 应用建议并测试

```bash
# 根据建议手动编辑配置文件
nano ~/moshi/final_config.yaml

# 测试新配置
roslaunch controller_ros turtlebot1.launch config:=~/moshi/final_config.yaml

# 验证改进
rostopic echo /controller/diagnostics | grep -E "lateral_error|alpha"
```

### 4. 迭代优化

如果还有问题，重复步骤 3 的增强诊断，直到满意。

---

## 📝 常见问题

### Q1: 增强诊断显示 "insufficient_data"

**原因**: 收集的数据不足（< 10 个样本）

**解决**: 增加诊断时间 `--duration 120`

### Q2: 增强诊断模块导入失败

**原因**: `enhanced_diagnostics.py` 不在 Python 路径中

**解决**:
```bash
# 确保文件在正确位置
ls ~/AllControlBase/controller_ros/scripts/enhanced_diagnostics.py

# 或添加到 PYTHONPATH
export PYTHONPATH=$PYTHONPATH:~/AllControlBase/controller_ros/scripts
```

### Q3: 补丁应用失败

**原因**: `unified_diagnostics.py` 已被修改

**解决**:
```bash
# 恢复备份
cp unified_diagnostics.py.backup_* unified_diagnostics.py

# 重新应用补丁
python3 apply_enhanced_diagnostics.py
```

### Q4: 诊断建议相互矛盾

**原因**: 多个参数相互影响

**解决**: 按优先级逐个调整，每次只改一个参数并测试

---

## 🔍 高级用法

### 独立运行增强诊断并保存结果

```bash
# 运行并保存建议到 YAML
rosrun controller_ros enhanced_diagnostics.py --duration 120

# 查看生成的建议文件
cat enhanced_diagnostics_suggestions.yaml
```

### 在 Python 代码中使用

```python
from enhanced_diagnostics import EnhancedDiagnostics

# 创建分析器
analyzer = EnhancedDiagnostics(window_size=200)

# 添加样本
for diag_msg in diagnostics_messages:
    analyzer.add_sample({
        'cmd_vx': diag_msg.cmd_vx,
        'cmd_omega': diag_msg.cmd_omega,
        'tracking_lateral_error': diag_msg.tracking_lateral_error,
        'alpha': diag_msg.alpha,
        # ...
    })

# 生成报告
print(analyzer.generate_report())

# 获取建议
suggestions = analyzer.get_all_suggestions()
```

---

## 📊 性能对比

### 标准诊断 vs 增强诊断

| 功能 | 标准诊断 | 增强诊断 |
|------|----------|----------|
| 传感器频率/延迟 | ✅ | ✅ |
| 底盘能力测试 | ✅ | ✅ |
| MPC 求解时间 | ✅ | ✅ |
| 横向跟踪误差 | ✅ | ✅ |
| **MPC 权重分析** | ❌ | ✅ |
| **一致性检查性能** | ❌ | ✅ |
| **控制平滑性** | ❌ | ✅ |
| **状态机切换** | ❌ | ✅ |
| **纵向跟踪误差** | ❌ | 🔄 (计划中) |
| **备用控制器测试** | ❌ | 🔄 (计划中) |

---

## 🚀 下一步计划

### 即将添加的功能

1. **纵向跟踪误差分析**
   - 分析速度跟踪精度
   - 建议 velocity 权重调整

2. **备用控制器主动测试**
   - 临时禁用 MPC
   - 测试备用控制器性能
   - 调优 lookahead_dist, kp_z, kp_heading

3. **TF 变换质量监控**
   - 检测 TF 超时频率
   - 分析坐标变换漂移

4. **EKF 估计质量分析**
   - 分析协方差和新息
   - 建议噪声参数调整

---

## 📞 支持

如有问题或建议，请参考：
- 主文档: `moshi/配置优化建议.md`
- 未诊断参数分析: `moshi/未诊断参数分析.md`
- 原始诊断工具: `controller_ros/scripts/unified_diagnostics.py`

---

**生成时间**: 2024-12-29  
**版本**: 1.0  
**作者**: Kiro Auto-generated
